import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.productcatalog',
  appName: 'Product Catalog',
  webDir: 'dist'
};

export default config;
